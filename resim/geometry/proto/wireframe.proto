syntax = "proto3";

package resim.geometry.proto;

message Wireframe {
    message Point {
        repeated double values = 1;
    }
    message Edge {
        uint32 start = 1;
        uint32 end   = 2;
    }

    repeated Point points = 1;
    repeated Edge  edges  = 2;
}
